<head>
    <% include header.ejs %>
</head>
<div class="title">
    <h1>Alle Dateien</h1>
    <p>Converted images</p>
    <% for (let img of converted) { %>
        <img src="../files/<%= img%>" style="width:100px">
    <% } %>
    <p>Original images</p>
    <% for (let img of original) { %>
        <img src="../files/<%= img%>" style="width:100px">
    <% } %>
</div>